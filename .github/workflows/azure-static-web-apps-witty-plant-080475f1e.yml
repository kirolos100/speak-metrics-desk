name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20' # or your preferred version

      - name: Install dependencies
        run: npm install

      - name: Build the app
        run: npm run build

      - name: Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          app_location: "."                  # repo root, not "src"
          output_location: "dist"           # built files go here
          api_location: ""
          action: "upload"
          skip_deploy_on_missing_secrets: true
          AZURE_STATIC_WEB_APPS_DEPLOYMENT_TOKEN: e4f0f69581df6686e31ca4e2c4be6a6576aef0a65bf14ed4fc6fa66b509d136501-be8c8340-482d-405f-8e08-fcdef92fef7d01e2927080475f1e



